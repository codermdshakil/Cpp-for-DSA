#include <bits/stdc++.h>

using namespace std;
int main()
{
    // dynamic array
    int *a = new int[5];

    // take array input
    for (int i = 0; i < 5; i++)
    {
        cin >> a[i];
    }

    // array output
    for (int i = 0; i < 5; i++)
    {
        cout << a[i] << endl;
    }

    return 0;
}