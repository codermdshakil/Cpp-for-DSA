#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    cin >> s;

    // print string character by character
    for (int i = 0; i < s.size(); i++)
    {
        // cout << s[i] << " ";
        // cout << s.at(i) << " "; - same like s[i]
    }

    // access first element of string
    // cout << s.front();  // H

    // access last element of string
    // cout << s.back();  // d
 

    return 0;
}